// Kept as a compatibility entry point. Database migrations are deliberately
// explicit and must not run implicitly during a Vercel build.
console.log('[setup-db] automatic database setup is disabled; use a reviewed, scoped migration runner.');
